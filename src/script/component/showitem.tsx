import * as React from "react";
import { Row, Col, Card, Icon } from 'antd';
import { History, Transaction, HistoryState, RecordState } from '../store/types';

export const ShowItem = () => {
  return (<div>showitem</div>)
}
